services:
  - type: web
    name: internship-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: MONGODB_URI
        scope: run
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000

  - type: static
    name: internship-frontend
    plan: free
    staticPublishPath: frontend/dist
    buildCommand: cd frontend && npm install && npm run build
    envVars:
      - key: VITE_API_URL
        value: https://internship-backend.onrender.com/api/internships
    routes:
      - path: /
        destination: /index.html
